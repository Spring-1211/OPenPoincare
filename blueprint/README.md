# Blueprint

This directory follows the standard `leanblueprint` source layout. Build locally with:

```bash
python3 -m pip install leanblueprint
leanblueprint web
leanblueprint pdf
leanblueprint checkdecls
```

The main source files live in `blueprint/src/`. The content is split by mathematical layer.
