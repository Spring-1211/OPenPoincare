import Poincare.Extinction.ForwardDifference

/-! # W₂ and extinction of π₂ -/

namespace Poincare.Extinction

/-- Minimal area invariant for nontrivial `π₂`, interface. -/
theorem W2_minArea_nontrivialPi2 : True := by
  sorry

/-- Forward difference inequality for `W₂`. -/
theorem W2_forwardDifference_inequality : True := by
  sorry

/-- Eventually `π₂` is trivial on remaining components, interface. -/
theorem pi2_eventually_trivial : True := by
  sorry

end Poincare.Extinction
