import Poincare.Perelman.ReducedLength

/-! # Reduced volume and monotonicity -/

namespace Poincare.Perelman

/-- Reduced volume definition placeholder. -/
def ReducedVolume : Prop := True

/-- Perelman reduced volume monotonicity interface. -/
theorem reducedVolume_monotone : True := by
  sorry

end Poincare.Perelman
